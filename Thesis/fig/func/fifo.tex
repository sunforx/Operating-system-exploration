%\input{printable}
\documentclass{standalone}
\usepackage{xeCJK}
\usepackage{tikz}
\usetikzlibrary{shapes.geometric, arrows.meta}

\begin{document}

\begin{tikzpicture}
  [auto,
  block/.style={rectangle, draw=black, thick,
  text width=8em,align=center, rounded corners,
  minimum height=4em},
  line/.style={draw, thick, shorten >=2pt, ->}]

  \matrix [column sep=5mm,row sep=7mm]
  {
  \node [block] (start) {start}; & \\
  \node [block] (init) {fifo32\_init()}; & \\
  \node [block] (put) {ififo32\_put()}; & \\
  \node [block] (status)   {fifo32\_status()}; & \\
  \node [block] (get) {fifo32\_get()}; & \\
  };
  \begin{scope}[every path/.style=line]
  \path (start) -- (init);
  \path (init) -- (put);
  \path (put) -- (status);
  \path (status) -- (get);
  \end{scope}
  \end{tikzpicture}

\end{document}

%%% Local Variables:
%%% mode: latex
%%% TeX-master: t
%%% End:
